version: 2
jobs:
  docker:
    - image: circleci/node@12.13.1
  working_directory: ~/repo
  
    steps:
      - checkout
       - run: npm install
       - run: npm test
workflows:
    build-and-test:
      jobs:
        - build-and-test